# F-002: Ayar Yüzeyi — Tipli Konfigürasyon Katmanı

**Status:** `REVIEW` — kod ve 252 otomatik test tamam
**Priority:** `P1 — High`
**Assigned Agent:** Claude Code (2026-08-13)
**Requested By:** Kadir Akar
**Target Date:** no deadline

---

## Goal

Kodun içine gömülü, davranışı belirleyen her değer tipli bir ayar hâline gelsin. Varsayılanlar
**bugünkü davranışı birebir korusun**; isteyen her şeyi değiştirebilsin.

Ayarlar, hem CLI'ın hem ileride yazılacak UI'ın (ADR-004) hem de aracın taşınacağı suite'in
bağlanacağı tek şema olur. `cli.py` bu şemanın ince bir sarmalayıcısına iner ve **mevcut CLI
kullanımı aynen korunur.**

Mimari gerekçe: ADR-003.

---

## Acceptance Criteria

- [x] **AC-1:** `Config()` — hiçbir alan verilmeden — bugünkü çıktının **birebir aynısını**
      üretir; koordinatlar, katmanlar, varlık tipleri ve metin içerikleri değişmez.
- [x] **AC-2:** Aşağıdaki katalogdaki her ayar TOML dosyasından okunabilir; anahtar adı alan
      adının aynısıdır.
- [x] **AC-3:** TOML'da tanınmayan bir bölüm ya da anahtar **hata**dır; çıktı üretilmeden durur
      ve hangi anahtarın tanınmadığını yazar. Sessizce yok sayılmaz — yazım hatası, ayarın
      uygulanmadığını fark ettirmeden geçemez.
- [x] **AC-4:** Tipi ya da aralığı geçersiz bir ayar (`frame_mm = "kalın"`, `min_width_factor = -1`)
      **hiçbir dosya yazmadan** durur ve `CONFIG_INVALID` kodlu bir mesaj basar.
- [x] **AC-5:** `--set <bölüm>.<anahtar>=<değer>` ile katalogdaki **her** ayara CLI'dan
      erişilebilir; bayrak tekrarlanabilir.
- [x] **AC-6:** Mevcut CLI bayraklarının tamamı çalışmaya devam eder ve aynı anlamı taşır:
      `--scale`, `--frame`, `--overflow`, `--text-style`, `--font`, `--layer-prefix`,
      `--dxf-version`, `--report`, `--config`, `--verbose`.
- [x] **AC-7:** Öncelik sırası: `--set` > adanmış CLI bayrağı > config dosyası > yerleşik
      varsayılan.
- [x] **AC-8:** `convert(job, config)` paket kökünden içe aktarılabilir ve `tkinter` ya da
      `tabletodxf.cli` yüklemeden çalışır.
      *Uygulama notu:* `argparse` **yükleniyor** — ama bizim kodumuzdan değil, `ezdxf` kendi
      içinde içe aktarıyor. Kontrolümüz dışında olduğu için ölçüt `tkinter` + `tabletodxf.cli`
      olarak daraltıldı; asıl iddia olan "kütüphane yolu CLI'a bağlı değil" korunuyor.
- [x] **AC-9:** `config.py` saf veridir: `odfpy`, `ezdxf`, `tkinter`, `numpy`, `fontTools` içe
      aktarmaz ve dosya I/O'sunu yalnızca kendisine verilen yol üzerinden yapar.
      *Uygulama notu:* bu **dosyanın** bağımsızlığıdır, paketin değil. `import tabletodxf.config`
      önce paketin `__init__`'ini çalıştırır, o da hattın tamamını (ve `ezdxf`'i) çeker. Test
      bu yüzden statik: `config.py`'nin kendi `import` satırlarına bakılıyor. Amaç, dosyanın
      suite'e olduğu gibi taşınabilmesi.
- [x] **AC-10:** TOML round-trip kayıpsızdır: `save(cfg)` → `load()` → `cfg` ile eşit.
- [x] **AC-11:** Testler — varsayılanların bugünkü çıktıyı koruduğu golden test, her bölüm için
      yükleme/doğrulama testleri, `--set` ve öncelik sırası testleri.

---

## Ayar Kataloğu

Tüm renkler `"#rrggbb"`. Tüm uzunluklar aksi yazmadıkça **mm**.

### `[source]` — kaynak `.ods` boşluk doldurma

`.ods`'in söylemediği şeyler için varsayılanlar. Sayfa bir değeri veriyorsa **her zaman sayfa
kazanır** (ADR-002); buradakiler yalnızca bilgi yokken devreye girer.

| Ayar | Tip | Varsayılan | Etki |
|---|---|---|---|
| `default_col_width_mm` | float | `22.58` | `.ods`'te tanımsız sütunun genişliği (Calc varsayılanı) |
| `default_row_height_mm` | float | `4.52` | Tanımsız satırın yüksekliği |
| `default_padding_mm` | float | `0.97` | `fo:padding` yoksa hücre iç boşluğu |
| `default_font_size_pt` | float | `10.0` | Font boyutu belirtilmemişse |
| `default_text_color` | renk | `#000000` | Metin rengi belirtilmemişse |
| `default_border_color` | renk | `#000000` | Kenarlık rengi belirtilmemişse |
| `borderless_width_pt` | float | `0.5` | `"solid #000"` gibi kalınlıksız kenarlığın kalınlığı. `0` yapılırsa böyle kenarlıklar çizilmez |
| `default_v_align` | `top`\|`middle`\|`bottom` | `bottom` | Dikey hizalama belirtilmemişse (Calc davranışı) |
| `align_numeric` | `left`\|`center`\|`right` | `right` | Açık hizalaması olmayan sayı/tarih/para hücresi |
| `align_boolean` | `left`\|`center`\|`right` | `center` | Açık hizalaması olmayan mantıksal hücre |
| `align_text` | `left`\|`center`\|`right` | `left` | Açık hizalaması olmayan metin hücresi |
| `stale_check_suffixes` | liste[str] | `[".xlsx", ".xls", ".xlsm"]` | `SRC_STALE` uyarısı için yanında aranan kardeş dosyalar. Boş liste kontrolü kapatır |

### `[layout]` — geometri

| Ayar | Tip | Varsayılan | Etki |
|---|---|---|---|
| `scale_cm_to_units` | float | `10.0` | 1 cm kaç çizim birimi |
| `frame_mm` | float | `0.35` | Dış çerçeve kalınlığı; `0` = çerçeve yok |
| `line_spacing` | float | `1.0` | Çok satırlı hücrede satır adımının em çarpanı |

### `[text]` — metin stili ve ölçüm

| Ayar | Tip | Varsayılan | Etki |
|---|---|---|---|
| `style_name` | str | `ONCU_TBL_TEXT` | DXF metin stili adı (hedef çizimdekini ezmemek için tekil) |
| `font_file` | str | `NotoSans-Regular.ttf` | Ölçüm ve stil için TTF; çıplak ad ya da tam yol |
| `fallback_cap_ratio` | float | `0.70` | Fontta `OS/2.sCapHeight` yoksa büyük harf yüksekliği oranı |

### `[overflow]` — hücreye sığmayan metin

| Ayar | Tip | Varsayılan | Etki |
|---|---|---|---|
| `mode` | `condense`\|`mtext`\|`marker`\|`full` | `condense` | Taşma davranışı (F-001) |
| `marker_char` | str | `#` | `marker` modunda kullanılan işaret |
| `min_width_factor` | float | `0.25` | `condense` modunda okunabilirlik tabanı |

### `[background]` — zemin

| Ayar | Tip | Varsayılan | Etki |
|---|---|---|---|
| `enabled` | bool | `true` | Seçimin tamamını kaplayan opak zemin çizilsin mi |
| `color` | renk | `#ffffff` | Zemin rengi |

### `[layers]` — katman adları ve renkleri

Katman adı = `prefix` + ilgili sonek.

| Ayar | Tip | Varsayılan | Etki |
|---|---|---|---|
| `prefix` | str | `ONCU_TBL` | Katman ad alanı |
| `grid_suffix` | str | `_GRID` | Kenarlık katmanı |
| `text_suffix` | str | `_TEXT` | Metin katmanı |
| `fill_suffix` | str | `_FILL` | Zemin ve dolgu katmanı |
| `overflow_suffix` | str | `_OVERFLOW` | Taşan hücre katmanı |
| `grid_color` | int (ACI) | `7` | Katman rengi |
| `text_color` | int (ACI) | `7` | Katman rengi |
| `fill_color` | int (ACI) | `7` | Katman rengi |
| `overflow_color` | int (ACI) | `1` | Katman rengi (kırmızı — `###` teşhis çıktısıdır) |

### `[output]` — DXF çıktısı

| Ayar | Tip | Varsayılan | Etki |
|---|---|---|---|
| `dxf_version` | str | `R2013` | R2013 ve üstü (AC-9 gereği; eskisi TTF stili taşımaz) |
| `insert_block_reference` | bool | `true` | Blok tanımının yanına model uzayına bir `INSERT` konsun mu |
| `block_base_point` | [float, float] | `[0.0, 0.0]` | Blok taban noktası |
| `write_report` | bool | `true` | Başarılı çalıştırmada `.report.txt` yazılsın mı |

---

## Ayarlanamayanlar

Bunlar bilinçli olarak sabit kalır. Tercih değil, doğruluk koşulu:

| Sabit | Neden |
|---|---|
| `PT_TO_MM = 25.4/72` | Birim tanımı |
| ODF satır/sütun tavanı (`1048576` / `16384`) | Format kısıtı |
| Geçerli DXF `lineweight` kümesi | Format kısıtı; ara değer dosyayı geçersiz kılar |
| Kayan nokta toleransı (`1e-9`) | AC-12 (determinizm); ayarlanabilir olması platformlar arası farkı geri getirir |
| `MTEXT` kaçış kuralları (`\`, `{`, `}`, `\P`) | Format kuralı; değiştirmek bozuk dosya üretir |
| Hata kodları (`SRC_FORMAT`, …) | Kararlı arayüz; betikler bunlara göre dallanıyor |
| Kenarlık tekilleştirme / eş doğrultulu birleştirme | Mimari karar (F-001), görünüm ayarı değil |
| Kaydırmada kelimenin bölünmemesi | Karar ve gerekçesi F-001'de; ayar yapmak iki davranışı da test etme yükü getirir |
| Dolgunun `HATCH` olması, zeminin en arkada olması | ADR kararları |

Bu tablo, "her şey ayar olsun" baskısına karşı bilinçli bir sınır. Yeni bir ayar önerildiğinde
önce buraya bakılır.

---

## CLI

Mevcut kullanım **değişmez.** Ek olarak:

```
--set <bölüm>.<anahtar>=<değer>      (tekrarlanabilir)
```

Örnekler:

```bash
tabletodxf mahal.ods --sheet Mahal --range B2:F40 --out t.dxf --block T \
    --set layers.prefix=PROJE_TBL \
    --set background.color=#f5f5f5 \
    --set overflow.min_width_factor=0.4
```

Öncelik: `--set` > adanmış bayrak > config dosyası > yerleşik varsayılan.

Adanmış bayrakların karşılıkları:

| Bayrak | Ayar |
|---|---|
| `--scale` | `layout.scale_cm_to_units` |
| `--frame` | `layout.frame_mm` |
| `--overflow` | `overflow.mode` |
| `--text-style` | `text.style_name` |
| `--font` | `text.font_file` |
| `--layer-prefix` | `layers.prefix` |
| `--dxf-version` | `output.dxf_version` |

---

## Kütüphane API'si

Suite'ten ve UI'dan kullanılacak yüzey (ADR-003):

```python
from tabletodxf import Config, Job, convert, load_config, save_config

config = load_config("tabletodxf.toml")        # ya da Config()
job = Job(
    source=Path("mahal.ods"),
    sheet="Mahal",
    range_text="B2:F40",
    out=Path("mahal.dxf"),
    block="MAHAL_TABLOSU",
)
result = convert(job, config)                   # Report enjekte edilebilir
print(result.out_path, result.warnings)
```

- `Config` — kalıcı ayarlar; UI'ın düzenlediği, TOML'a yazılan
- `Job` — çalıştırmaya özgü girdiler; UI'ın her seferinde doldurduğu form
- `convert()` — CLI, UI ve suite için **tek** giriş noktası
- `Result` — çıktı yolu, rapor yolu, uyarı sayısı, varlık sayıları

Ayrım ADR-003'te gerekçelendirildi: ikisinin ömrü farklı.

---

## Test Plan

### Unit Tests

- [x] `Config()` alan alan bugünkü sabitlerle eşleşiyor
- [x] Her bölüm TOML'dan yükleniyor; anahtar adı = alan adı
- [x] Bilinmeyen bölüm → `CONFIG_INVALID`, hangi anahtar olduğu mesajda
- [x] Bilinmeyen anahtar → `CONFIG_INVALID`
- [x] Tip hatası (`frame_mm = "kalın"`) → `CONFIG_INVALID`
- [x] Aralık hatası (`min_width_factor = -1`, `frame_mm < 0`) → `CONFIG_INVALID`
- [x] Renk ayrıştırma: `"#ffffff"` ve geçersiz değer
- [x] `Literal` alanlar (`overflow.mode`, hizalamalar) geçersiz değeri reddediyor
- [x] TOML round-trip kayıpsız (AC-10)
- [x] `--set` her bölüme erişiyor; tekrarlanan bayrak birikiyor
- [x] Öncelik: `--set` > bayrak > dosya > varsayılan
- [x] `config.py` `odfpy`/`ezdxf`/`tkinter` içe aktarmıyor (import denetimi)

### Integration Tests

- [x] **Varsayılan golden:** `Config()` ile üretilen DXF, F-001 golden testinin beklediğiyle
      birebir aynı (AC-1)
- [x] Değiştirilen ayar çıktıya yansıyor: `layers.prefix`, `background.enabled=false`,
      `output.insert_block_reference=false`, `overflow.mode`
- [x] Geçersiz config → hiçbir dosya yazılmıyor (AC-4 + F-001 AC-10)
- [x] `convert()` `argparse`/`tkinter` yüklemeden çalışıyor (AC-8)

---

## Agent Notes

### Open Questions

- [x] **Ayar profilleri.** Ofiste birden çok tablo tipi varsa (mahal listesi, çizim listesi,
      mahal mahal metraj) her biri farklı ayar isteyebilir. Bugün tek `tabletodxf.toml` var.
      Profil desteği (`--profile cizim-listesi`) gerekecek mi, yoksa proje klasörü başına bir
      config yeterli mi? **UI yazılmadan önce netleşmeli**, çünkü UI'ın ayar kaydetme akışı
      buna bağlı.
- [x] **Ayar dosyasının aranma sırası.** Bugün yalnızca `./tabletodxf.toml`. Kullanıcı başına
      (`%APPDATA%`) bir varsayılan ayar dosyası da aranmalı mı? UI kurulduğunda ayarların
      nerede yaşayacağı sorusu bununla aynı.

### Decisions Made

| Decision | Rationale | Date |
|---|---|---|
| Tipli, gruplanmış `Config` | ADR-003 | 2026-08-13 |
| `Config` / `Job` ayrımı | İkisinin ömrü farklı: biri kalıcı tercih, diğeri her çalıştırmada dolan form | 2026-08-13 |
| Varsayılanlar bugünkü davranış | Mevcut çıktı regresyona uğramasın; `Config()` bir golden testle sabitlenir | 2026-08-13 |
| Bilinmeyen anahtar hata | Sessiz yok sayma, ayarın uygulanmadığını fark ettirmez | 2026-08-13 |
| `--set` kaçış kapısı | 40+ adanmış bayrak `--help`'i okunamaz kılardı; CLI yine de her ayara erişmeli | 2026-08-13 |
| Bazı sabitler ayar **değil** | Doğruluk koşulu olanlar ayarlanabilir olmamalı; bkz. "Ayarlanamayanlar" | 2026-08-13 |

---

## Related

- ADR-003: Ayarları Tipli Bir Konfigürasyon Katmanına Taşı
- ADR-004: UI Araç Seti ve Paketleme
- `DOCS/Features/F-001.md` — varsayılanların eşit olması gereken mevcut davranış
